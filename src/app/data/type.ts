export type skillprops = {
  language: string;
  role: string;
  client: string;
  period: string;
  technology: string;
};

export type PortfolioDetailProps = {
  id: string;
  title: string;
  image: string;
  tags: string[];
  url: string;
};